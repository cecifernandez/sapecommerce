<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="checkoutDialog"
        title="{i18n>checkoutTitle}"
        class="sapUiResponsivePadding"
        stretch="{device>/system/phone}"
    >
        <content>
            <VBox id="checkoutVBox" class="sapUiSmallMargin">
                <HBox
                    id="checkoutNameHBox"
                    alignItems="Center"
                    class="sapUiTinyMarginTop"
                >
                    <core:Icon
                        id="checkoutNameIcon"
                        src="sap-icon://customer"
                        class="sapUiSmallMarginEnd"
                    />
                    <Label
                        id="checkoutNameLabel"
                        text="{i18n>name}"
                        required="true"
                    />
                </HBox>
                <Input
                    id="checkoutNameInput"
                    value="{checkoutModel>/name}"
                    placeholder="{i18n>namePlaceholder}"
                />
                <HBox
                    id="checkoutEmailHBox"
                    alignItems="Center"
                    class="sapUiTinyMarginTop"
                >
                    <core:Icon
                        id="checkoutEmailIcon"
                        src="sap-icon://arobase"
                        class="sapUiSmallMarginEnd"
                    />
                    <Label
                        id="checkoutEmailLabel"
                        text="{i18n>email}"
                        required="true"
                    />
                </HBox>

                <Input
                    id="checkoutEmailInput"
                    value="{checkoutModel>/email}"
                    placeholder="{i18n>emailPlaceholder}"
                    type="Email"
                />

                <HBox
                    id="checkoutAddressHBox"
                    alignItems="Center"
                    class="sapUiTinyMarginTop"
                >
                    <core:Icon
                        id="checkoutAddressIcon"
                        src="sap-icon://map"
                        class="sapUiSmallMarginEnd"
                    />
                    <Label
                        id="checkoutAddressLabel"
                        text="{i18n>address}"
                        required="true"
                    />
                </HBox>
                <Input
                    id="checkoutAddressInput"
                    value="{checkoutModel>/address}"
                    placeholder="{i18n>addressPlaceholder}"
                />

                <HBox
                    id="checkoutCardHBox"
                    alignItems="Center"
                    class="sapUiTinyMarginTop"
                >
                    <core:Icon
                        id="checkoutCardIcon"
                        src="sap-icon://credit-card"
                        class="sapUiSmallMarginEnd"
                    />
                    <Label
                        id="checkoutCardLabel"
                        text="{i18n>card}"
                        required="true"
                    />
                </HBox>
                <Input
                    id="checkoutCardInput"
                    value="{checkoutModel>/card}"
                    placeholder="{i18n>cardPlaceholder}"
                    type="Number"
                />

                <HBox
                    id="checkoutDniHBox"
                    alignItems="Center"
                    class="sapUiTinyMarginTop"
                >
                    <core:Icon
                        id="checkoutDniIcon"
                        src="sap-icon://business-card"
                        class="sapUiSmallMarginEnd"
                    />
                    <Label
                        id="checkoutDniLabel"
                        text="{i18n>dni}"
                        required="true"
                    />
                </HBox>
                <Input
                    id="checkoutDniInput"
                    value="{checkoutModel>/dni}"
                    placeholder="{i18n>dniPlaceholder}"
                    type="Number"
                />
            </VBox>
        </content>
        <beginButton>
            <Button
                id="checkoutConfirmButton"
                text="{i18n>confirmPurchase}"
                press=".onConfirmCheckout"
                type="Emphasized"
            />
        </beginButton>
        <endButton>
            <Button
                id="checkoutCancelButton"
                text="{i18n>cancel}"
                press=".onCancelCheckout"
                type="Transparent"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
